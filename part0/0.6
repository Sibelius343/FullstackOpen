note over User:
User types note in
text field and clicks
submit button
end note

note over User:
Event handler in js
adds new note to list
and rerenders the notes
with new note added
end note

User->Server: HTTP POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa

Server->User: 201 created
